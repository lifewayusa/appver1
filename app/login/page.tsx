'use client';

import TemplatePages from '../components/TemplatePages';
import AuthForm from "../components/AuthForm";

// Página de login desativada: autenticação removida para ambiente local.
export default function LoginPage() {
  return <div>Login desativado para ambiente local.</div>;
}
